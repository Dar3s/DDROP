services:
  web:
    build: .
    ports:
      - "8080:80"
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: dropshipping_app
      DB_USER: ddrop_user
      DB_PASS: ddrop_password
    depends_on:
      - db

  db:
    image: mariadb:11.4
    environment:
      MARIADB_ROOT_PASSWORD: rootpassword
      MARIADB_DATABASE: dropshipping_app
      MARIADB_USER: ddrop_user
      MARIADB_PASSWORD: ddrop_password
    ports:
      - "3307:3306"
